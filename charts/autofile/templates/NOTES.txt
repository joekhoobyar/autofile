1. Get the application URL by running these commands:
{{- if .Values.ingress.enabled }}
{{- range $host := .Values.ingress.hosts }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host.host }}
{{- end }}
{{- else }}
  kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{ include "autofile.ui.fullname" . }} 8080:{{ .Values.ui.service.port }}
  kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{ include "autofile.api.fullname" . }} 8000:{{ .Values.api.service.port }}
{{- end }}
