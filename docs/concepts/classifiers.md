# Classifiers

Classifier blocks support document classification workflows.

## Classifier Blocks

A classifier block represents classification logic that can be evaluated against documents. Classifier blocks can help determine document type, routing, or other derived classification outcomes.

## Testing Classification

The UI includes classifier testing for documents so rules can be evaluated before relying on them in a workflow.

Classifier behavior is expected to evolve while Autofile remains alpha software.
